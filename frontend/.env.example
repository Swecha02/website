# URL of your backend API (see /backend). On the VPS this will typically be
# served on the same domain behind Nginx, e.g. https://yourswecha.com
# For local development, point it at your local backend:
VITE_API_URL=http://localhost:4000
