# Overrides .env when running `npm run build` (Vite mode=production).
# Keeps production builds pointed at the live domain regardless of what
# .env is set to for local dev against a local backend.
VITE_API_URL=https://yourswecha.com
